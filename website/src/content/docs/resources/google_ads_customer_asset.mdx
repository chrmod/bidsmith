---
title: google_ads_customer_asset
description: Links an asset (e.g. a call extension) to all campaigns in the account.
---

import { Aside } from '@astrojs/starlight/components';
import AttributeTable from '~/components/AttributeTable.astro';
import schema from '~/data/schema.json';

export const resource = schema.resources['google_ads_customer_asset'];

A **customer asset** links an asset to the *entire account*. Once linked,
the asset can appear with any ad in the account that's eligible to show
it. It's the account-wide counterpart to
[`google_ads_campaign_asset`](./google_ads_campaign_asset/) (one campaign)
and [`google_ads_ad_group_asset`](./google_ads_ad_group_asset/) (one ad
group).

<Aside type="note">
  The `asset` reference can point at any asset type bidsmith models —
  [`google_ads_call_asset`](./google_ads_call_asset/),
  [`google_ads_sitelink_asset`](./google_ads_sitelink_asset/),
  [`google_ads_callout_asset`](./google_ads_callout_asset/), or
  [`google_ads_structured_snippet_asset`](./google_ads_structured_snippet_asset/) —
  and `field_type` must match the asset's kind. Call extensions are the
  most common account-wide link.
</Aside>

## Example

Link the `main_phone` call asset to every ad in the account:

```hcl
resource "google_ads_customer_asset" "main_phone_link" {
  asset      = google_ads_call_asset.main_phone.id
  field_type = "CALL"
  status     = "ENABLED"
}
```

## Removing ones you didn't declare

Because these links reach every campaign, bidsmith will not remove a
live one just because your files stop mentioning it. Ask for that in the
[`provider`](./provider/) block:

```hcl
provider "google_ads" {
  owns = ["sitelinks", "callouts"]
}
```

Each kind you list becomes exhaustive: the account-level sitelinks are
exactly the `google_ads_customer_asset` blocks declaring one, and a live
sitelink nothing declares shows up as `- destroy` in
[`bidsmith plan`](/bidsmith/commands/plan/). See
[Serve only the extensions you declared](/bidsmith/recipes/serve-only-the-extensions-you-declared/).

## Schema

<AttributeTable block={resource} headingLevel={3} />

## See also

- [`google_ads_call_asset`](./google_ads_call_asset/) — the typical `asset` reference.
- [`google_ads_campaign_asset`](./google_ads_campaign_asset/) — link an asset to one campaign instead of the whole account.
- [`google_ads_ad_group_asset`](./google_ads_ad_group_asset/) — link an asset to one ad group.
